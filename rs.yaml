# ReplicaSet configuration for Nginx pods
apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: nginx-rs
  labels:
    env: demo

spec:
  replicas: 3
  selector:
    matchLabels:
      env: demo-app
      type: frontend
  template:
    metadata:
      name: nginx-pod
      labels:
        env: demo-app
        type: frontend
    spec:
      containers:
        - name: nginx-container
          image: nginx:latest
          ports:
            - containerPort: 80
